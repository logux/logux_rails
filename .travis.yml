sudo: false
language: ruby
rvm:
  - 2.4.2

before_install: gem install bundler -v 1.16.0

before_script:
  - bundle install
  - bundle exec appraisal

script:
  - bundle exec appraisal rspec
  - bundle exec rubocop
